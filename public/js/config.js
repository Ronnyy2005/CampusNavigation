// if backend and frontend are same origin:
window.API_BASE = "http://localhost:5500"; // e.g., "", or "http://localhost:3000"

// if your API runs on a different port, set: window.API_BASE = "http://localhost:3000";
